// Legacy import path — forwards to the unified Brutalist Pop navbar so every page
// (auth, create-listing, messages, profile, etc.) renders the same chrome.
import UpdatedNavbar from './UpdatedNavbar';

const Navbar = () => <UpdatedNavbar />;

export default Navbar;
